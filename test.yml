- name: Copy File to User's Desktop
  hosts: localhost
  become: true

  tasks:
    - name: Copy file to user's desktop
      copy:
        src: ./file.txt
        dest: "{{ ansible_env.HOME }}/Desktop/file.txt"
        remote_src: yes
